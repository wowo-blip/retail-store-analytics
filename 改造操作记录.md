# 零售门店项目：逐步改造记录

日期：2026-09-07。目录：`F:\codex\retail-store-analytics`。原版复现保留在相邻的 `streamlit-sales-dashboard` 文件夹。

## 第1步：检查现有环境并确定结构

检查 Python、Git、MySQL、Docker 及占用端口。发现已安装 MySQL 8.4.9，系统 MySQL84 服务正在运行，3307和8502可用；因此复用已安装的服务器程序，建立本项目专用实例。
创建新项目目录，复制源Excel到 `data/raw/supermarket_sales.xlsx`，校验副本哈希与原文件一致。
通过已安装的 uv 建立 Python 3.11.16 虚拟环境。

```powershell
& 'F:\codex\.runtimes\bootstrap\Scripts\uv.exe' venv --python 3.11 --seed 'F:\codex\retail-store-analytics\.venv'
```

## 第2步：安装依赖、初始化MySQL

安装 `requirements.txt` 中的数据处理、可视化、SQLAlchemy、PyMySQL、密码认证和测试依赖；最终版本固定在 `requirements-lock.txt`。

```powershell
& 'F:\codex\.runtimes\bootstrap\Scripts\uv.exe' pip install --python .venv\Scripts\python.exe -r requirements.txt
```

初始化独立数据库目录：

```powershell
mysqld --no-defaults --initialize-insecure --basedir=D:/MySQL --datadir=F:/codex/retail-store-analytics/.runtime/mysql-data --console
```

随后后台启动3307实例，仅监听127.0.0.1，禁用X协议和二进制日志，本地样例使用128MB缓冲池。初始化后立即运行：

```powershell
.\.venv\Scripts\python.exe scripts\bootstrap_db.py
```

脚本核对服务器数据目录，随机设置本项目root密码，创建 `retail_analytics` 数据库、`retail_etl` 导入账户和 `retail_reader` 只读账户。密码保存到忽略提交的本地配置，没有输出到报告。

## 第3步：建立校验与导入流程

编写 `sql/schema.sql` 和 `retail/etl.py`。整理字段名与类型，检查空值、重复编号、非有限数值、非正件数、负金额、评分范围、税额/总额关系、客户类型和门店城市映射。
校验失败时输出拒绝行和问题报告，不写业务表；全部通过后使用事务插入。金额使用DECIMAL，源文件哈希用于识别重复导入。

```powershell
.\.venv\Scripts\python.exe -m retail.etl
.\.venv\Scripts\python.exe -m retail.etl
```

第一次返回 inserted，第二次返回 already_loaded。两次均为1000行，金额322966.7490，未重复累计。

## 第4步：编写SQL指标

在 `sql/metrics.sql` 编写KPI、门店、日期、品类、交叉分析、月份和明细七组查询。用窗口函数计算销售额占比和排名，通过SQLAlchemy参数绑定实现统一筛选。
核对结果：1000笔交易、5510件商品、89个日历日；金额322966.7490；平均交易金额322.966749。

## 第5步：实现门店差异与统计分析

在 `retail/statistics.py` 实现：

- 交易筆数×平均交易金额的对称分解；
- 合并样本品类交易占比标准化；
- 最高1%交易敏感性分析；
- 共同日期簇Bootstrap，5000次，种子20260907；
- 7日循环移动块敏感性检查，以及样本不足保护。

```powershell
.\.venv\Scripts\python.exe -m retail.report
```

报告、SQL汇总和Bootstrap结果写入 `reports`。Naypyitaw−Yangon销售额差4368.34；笔数项约−3896.72，平均交易金额项约8265.06。三组均值差95%日期簇区间均包含0，报告保留这一结果。

## 第6步：重新实现中文交互看板

在 `app.py` 建立经营总览、门店差异、统计与数据三个页签。加入日期、城市、客户类型与品类筛选、六项KPI、趋势/均线、热力图、分解瀑布图、箱线图、月份比较和区间图。
所有业务指标从MySQL查询；统计使用同一筛选后的明细。加入当前筛选CSV、当前区间结果及完整样本报告下载。
在 `.streamlit/config.toml` 设置统一主题，修正旧版Streamlit指标卡DOM选择器，使指标卡样式实际生效。

```powershell
.\.venv\Scripts\python.exe scripts\start_local.py --no-browser
```

本地地址：http://127.0.0.1:8502 。添加一键启动、停止、重算报告脚本。

## 第7步：测试与浏览器验收

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m pip check
```

16项自动测试通过，覆盖错误数据拒绝、金额精度、SQL汇总一致性、组合筛选、空筛选、日期边界、参数注入防护、只读权限、分解恒等式和Bootstrap重现性。
兼容环境中的Pandas/NumPy会发出弃用提示，未导致测试失败。
使用独立Edge浏览器检查三个页签、真实图表数据、日期与组合筛选、空结果、无效日期范围和CSV下载，并保存 `reports/browser-checks.json` 与界面截图。
浏览器验收中日期选择弹层曾遮挡下一次点击，测试流程增加关闭日历后再操作；这属于自动化交互调整。
视觉检查后为瀑布图增加上下留白，并统一不同图表中各城市的颜色。完整停止、重新启动和重新执行首次配置脚本也已验证，数据仍为1000行，电脑原有MySQL84服务保持运行。

## 第8步：整理学习与简历材料

编写README、数据来源与字典、指标/统计方法说明、生成式经营分析报告，以及简历和面试说明。保留开源来源与AI协助开发说明；不写未发生的业务增长或企业落地成果。
本地项目不包含云部署，不需要外部API或付费服务。
